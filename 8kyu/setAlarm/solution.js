/**
 * Task: return true jika employed bernilai true dan vacationnya bernilai false, selain itu return false.
 */
function solution(employed, vacation) {
  return employed && !vacation ? true : false;
}
