/**
 * Task: convert binary ke desimal
 *
 * - Dalam hal ini, kita bisa memanfaatkan parseInt().
 * - Seandainya jika desimal ke binary, maka kita bisa memanfaatkan toString()
 */
function solution(arr) {
  return parseInt(arr.join(), 2);
}
